<Window x:Class="SimpleCalculatorMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MVVM Calculator" Height="450" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding SelectedOperationSymbol}" 
                   HorizontalAlignment="Right" 
                   FontSize="14" Foreground="Gray" Margin="0,0,5,0"/>

        <TextBlock Grid.Row="1" Text="{Binding Display}" 
                   FontSize="32" TextAlignment="Right" 
                   Background="#F0F0F0" Margin="0,5,0,10" Padding="5"/>

        <UniformGrid x:Name="ButtonsContainer" Grid.Row="2" Columns="4" Rows="5"/>
    </Grid>
</Window>